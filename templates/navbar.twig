<!-- Navigation -->
<nav class="fixed top-0 left-0 w-full z-50 backdrop-blur-lg bg-white bg-opacity-60 border-b border-gray-200 px-8 py-3 flex items-center justify-between shadow-lg">
    <!-- Logo and Brand -->
    <div class="flex items-center space-x-3">
        <span class="text-2xl font-extrabold text-gray-800 tracking-tight" style="font-family: 'Noto Sans', sans-serif;">Jörg Feller</span>
    </div>
    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center space-x-8">
        <a href="/{{ lang ?? 'en' }}/" class="text-gray-800 hover:text-blue-600 px-3 py-2 rounded-md text-base font-semibold transition">
            {% if lang == 'de' %}Startseite{% else %}Home{% endif %}
        </a>
        <a href="/{{ lang ?? 'en' }}/about" class="text-gray-800 hover:text-blue-600 px-3 py-2 rounded-md text-base font-semibold transition">
            {% if lang == 'de' %}Über mich{% else %}About{% endif %}
        </a>
        <a href="/{{ lang ?? 'en' }}/contact" class="text-gray-800 hover:text-blue-600 px-3 py-2 rounded-md text-base font-semibold transition">
            {% if lang == 'de' %}Kontakt{% else %}Contact{% endif %}
        </a>
    </div>
    <!-- Language Dropdown -->
    <div class="hidden md:flex items-center">
        {% include 'lang-switch.twig' %}
    </div>
    <!-- Mobile menu button (minimalist) -->
    <div class="md:hidden flex items-center">
        <button class="text-gray-800 hover:text-blue-600 focus:outline-none">
            <svg class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </button>
    </div>
</nav>
<!-- Spacer to prevent content from being hidden under fixed navbar -->
<div class="h-20"></div> 