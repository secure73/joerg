<!-- Navigation -->
<nav class="bg-white bg-opacity-80 border-b border-pink-100 shadow-xl rounded-b-3xl mx-auto max-w-3xl mt-6 mb-8 px-6 py-2 flex items-center justify-between floating-navbar" style="box-shadow: 0 4px 24px 0 rgba(249,197,209,0.15);">
    <!-- Logo and Brand with Sakura Icon -->
    <div class="flex items-center space-x-3">
        <svg width="32" height="32" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="32" cy="32" r="28" fill="#F9C5D1"/>
            <path d="M32 12 Q36 24 48 20 Q40 32 52 36 Q40 36 44 48 Q32 40 20 48 Q24 36 12 36 Q24 32 16 20 Q28 24 32 12 Z" fill="#F7A1C4"/>
        </svg>
        <a href="/{{ lang ?? 'en' }}/" class="text-2xl font-extrabold text-pink-700 tracking-tight hover:text-pink-900 transition">Jörg Feller</a>
    </div>
    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center space-x-6">
        <a href="/{{ lang ?? 'en' }}/" class="text-gray-700 hover:text-pink-700 px-3 py-2 rounded-md text-base font-medium transition">
            {% if lang == 'de' %}Startseite{% else %}Home{% endif %}
        </a>
        <a href="/{{ lang ?? 'en' }}/about" class="text-gray-700 hover:text-pink-700 px-3 py-2 rounded-md text-base font-medium transition">
            {% if lang == 'de' %}Über mich{% else %}About{% endif %}
        </a>
        <a href="/{{ lang ?? 'en' }}/contact" class="text-gray-700 hover:text-pink-700 px-3 py-2 rounded-md text-base font-medium transition">
            {% if lang == 'de' %}Kontakt{% else %}Contact{% endif %}
        </a>
    </div>
    <!-- Language Dropdown -->
    <div class="hidden md:flex items-center">
        {% include 'lang-switch.twig' %}
    </div>
    <!-- Mobile menu button (minimalist) -->
    <div class="md:hidden flex items-center">
        <button class="text-pink-700 hover:text-pink-900 focus:outline-none">
            <svg class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </button>
    </div>
</nav>
<style>
    .floating-navbar {
        backdrop-filter: blur(8px);
    }
</style> 