<!-- Navigation -->
<nav class="bg-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16">
            <!-- Logo and Brand -->
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <a href="/{{ lang ?? 'en' }}/" class="text-xl font-bold text-gray-800">STCMS</a>
                </div>
            </div>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="/{{ lang ?? 'en' }}/" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                    {% if lang == 'de' %}Startseite{% else %}Home{% endif %}
                </a>
                <a href="/{{ lang ?? 'en' }}/react" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                    {% if lang == 'de' %}React Beispiel{% else %}React Example{% endif %}
                </a>
                <a href="/{{ lang ?? 'en' }}/how" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                    {% if lang == 'de' %}Anleitung{% else %}How To{% endif %}
                </a>
                <a href="/{{ lang ?? 'en' }}/login" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                    {% if lang == 'de' %}Anmelden{% else %}Login{% endif %}
                </a>
            </div>

            <!-- Language Dropdown -->
            <div class="hidden md:flex items-center">
                {% include 'lang-switch.twig' %}
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden flex items-center">
                <button class="text-gray-600 hover:text-gray-900 focus:outline-none focus:text-gray-900">
                    <svg class="h-6 w-6" fill="none" viewbox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</nav> 